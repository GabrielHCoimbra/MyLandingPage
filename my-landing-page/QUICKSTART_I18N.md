# ⚡ Quick Start i18n - 5 Minutos\n\n## 1️⃣ Verificar Instalação\n\n```bash\ncd my-landing-page\nnpm list i18next react-i18next\n```\n\nDevem estar instalados (se não estiverem):\n```bash\nnpm install i18next react-i18next\n```\n\n---\n\n## 2️⃣ Testar Float Box\n\n1. Abra seu site\n2. Procure as **4 bandeiras** no canto inferior direito\n3. Clique em uma bandeira diferente\n4. ✅ O site inteiro deve mudar de idioma!\n\n---\n\n## 3️⃣ Verificar Traduções Funcionando\n\nAbra o navegador DevTools (F12) e execute:\n\n```javascript\n// Verificar idioma atual\nI18NextHttpBackend?.getLanguage?.() // ou\nlocalStorage.getItem('language')\n```\n\nVocê deve ver: `\"en\"`, `\"pt\"`, `\"fr\"` ou `\"es\"`\n\n---\n\n## 4️⃣ Adicionar Tradução em 3 Passos\n\n### Passo 1: Abrir arquivos JSON\n\nAbra em paralelo:\n- `src/locales/en.json`\n- `src/locales/pt.json`\n- `src/locales/fr.json`\n- `src/locales/es.json`\n\n### Passo 2: Adicionar chave em TODOS\n\n**en.json:**\n```json\n\"newSection\": {\n  \"myKey\": \"Hello World\"\n}\n```\n\n**pt.json:**\n```json\n\"newSection\": {\n  \"myKey\": \"Olá Mundo\"\n}\n```\n\nRepita para `fr.json` e `es.json`\n\n### Passo 3: Usar no Componente\n\n```jsx\nimport { useTranslation } from 'react-i18next';\n\nexport default function MyComponent() {\n  const { t } = useTranslation();\n  return <p>{t('newSection.myKey')}</p>;\n}\n```\n\n---\n\n## 5️⃣ Estrutura de Ficheiros (Resumo)\n\n```\nsrc/\n├── i18n.js                      ← Config\n├── locales/                     ← 4 arquivos de tradução\n│   ├── en.json\n│   ├── pt.json\n│   ├── fr.json\n│   └── es.json\n└── components/\n    └── LanguageSwitcher/        ← Float box com bandeiras\n        ├── LanguageSwitcher.jsx\n        └── LanguageSwitcher.css\n```\n\n---\n\n## 6️⃣ Comandos Úteis\n\n```bash\n# Verificar se tudo foi instalado\nnpm list react-i18next\n\n# Procurar por chaves não traduzidas\ngrep -r \"t('\" src/ | wc -l\n\n# Listar todos os JSON files\nls -la src/locales/\n```\n\n---\n\n## 7️⃣ Troubleshooting Rápido\n\n### Problema: Float box não aparece\n**Solução:** Verifique se `App.jsx` contém:\n```jsx\nimport LanguageSwitcher from './components/LanguageSwitcher/LanguageSwitcher'\n\nexport default function App() {\n  return (\n    <>\n      <Home />\n      <LanguageSwitcher /> {/* ← Deve estar aqui */}\n    </>\n  )\n}\n```\n\n### Problema: Traduções não funcionam\n**Solução:** Veja se `main.jsx` tem:\n```jsx\nimport './i18n' // ← Deve estar no topo\n```\n\n### Problema: \"i18n is not defined\"\n**Solução:** Instale dependências:\n```bash\nnpm install i18next react-i18next\nnpm install\n```\n\n---\n\n## 8️⃣ Exemplos de Uso\n\n### Traduzir um Título\n```jsx\nconst { t } = useTranslation();\nreturn <h1>{t('skills.title')}</h1>; // \"Skills\" / \"Habilidades\" / etc\n```\n\n### Traduzir um Parágrafo\n```jsx\nconst { t } = useTranslation();\nreturn <p>{t('about.description')}</p>;\n```\n\n### Mudar Idioma Manualmente\n```jsx\nconst { i18n } = useTranslation();\n<button onClick={() => i18n.changeLanguage('pt')}>\n  Português\n</button>\n```\n\n---\n\n## 9️⃣ Checklists de Verificação\n\n### ✅ Instalação\n- [ ] `npm install i18next react-i18next` executado\n- [ ] `src/i18n.js` existe\n- [ ] `src/locales/` tem 4 arquivos JSON\n\n### ✅ Integração\n- [ ] `main.jsx` importa i18n\n- [ ] `App.jsx` contém LanguageSwitcher\n- [ ] `LanguageSwitcher/` existe em components\n\n### ✅ Componentes\n- [ ] `Navbar.jsx` usa `useTranslation()`\n- [ ] `Section.jsx` usa `useTranslation()`\n- [ ] `Experience.jsx` usa `useTranslation()`\n- [ ] `Project.jsx` usa `useTranslation()`\n\n### ✅ Funcionalidade\n- [ ] Float box aparece no canto inferior direito\n- [ ] Clique nas bandeiras muda o idioma\n- [ ] Página inteira é traduzida\n- [ ] Preferência fica salva no localStorage\n\n---\n\n## 🔟 Próximo Passo\n\n1. Se tudo funciona: **Deploy!** 🚀\n2. Se algo não funciona: Leia `I18N_COMPLETE_GUIDE.md`\n3. Para mais exemplos: Veja `I18N_EXAMPLES.md`\n\n---\n\n**Tempo estimado para implementar:** 5-10 minutos  \n**Dificuldade:** ⭐ Fácil  \n**Status:** ✅ Pronto para usar  \n\n"
